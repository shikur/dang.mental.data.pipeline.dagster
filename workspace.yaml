load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: pipeline_mentalhealth
      port: 4000
      location_name: "pipeline_mentalhealth"

  - grpc_server:
     host: pipeline_conversation_mentalhealth
     port: 4047
     location_name: "pipeline_conversation_mentalhealth"      

